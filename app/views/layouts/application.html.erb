<!DOCTYPE html>
<html>
<head>
  <title>Musicals</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  
<body>
  
  <nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-brand" ><%= link_to "Musicals", performances_path %></p>
      <ul class="nav navbar-nav navbar-left" id="home">
        <li><%= link_to "All shows", shows_path %></li>
        <li><%= link_to "All theatres", theatres_path %></li>
          
       
      </ul>
    </div>
    
    <div class="col-md-offset-5">
			<% if params[:controller] == "shows" && params[:action] == "index" %>
        <form class="navbar-form navbar-left" role="search">
          <div class="form-group">
           <%= form_tag(shows_path, :method => "get", class: "navbar-form") do %>
           <%= text_field_tag :search, params[:search], placeholder: "Search Shows" %>
           </div>
              <button class="btn btn-primary" type="submit">Search</button>
           <% end %>
        </form>
			<% elsif params[:controller] == "theatres" && params[:action] == "index"%>
        <form class="navbar-form navbar-left" role="search">
            <div class="form-group">
              <%= form_tag(theatres_path, :method => "get", class: "navbar-form") do %>
              <%= text_field_tag :search, params[:search], placeholder: "Search Theatres" %>
             </div>
                <button class="btn btn-primary" type="submit">Search</button>
             <% end %>
         </form>
      <% end %>
     </div>
      
      
          
    <!-- Collect the nav links, forms, and other content for toggling -->
  
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right" id="home">
        <li><%= link_to "Add a new performance", new_performance_path %></li>
        <li><%= link_to "Make a change", perfchange_path %></li>
        <% if current_visitor %>
        <li><%= link_to "Log Out", logout_path, method: "delete" %></li>
        <% else %>
          <li><%= link_to "Log In", 'login' %> </li>
          
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
    
  </div><!-- /.container-fluid -->
</nav>
  
  <div class="flash"><%= render 'layouts/messages' %></div>
      
  

<%= yield %>

  </body>
  
  <footer class="footer">
    <div class=container>
      
       
      
    </div>
  </footer>
  
</html>

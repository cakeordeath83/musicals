<h1>Index for Shows page</h1>

<% @shows.each do |show| %>
<%= show.name %>
<p> Number of performances = <%= @performances.find_all{|p| p.show_id == show.id}.count %></p>
<! Pretty sure that there should be a better way of doing this! !>
<%= link_to "Edit", edit_show_path(show) %><br>
<% end %>

<%= link_to "Add new show", new_show_path %>